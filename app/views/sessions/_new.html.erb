<%= simple_form_for(:session, url: sessions_path) do |l| %>
<%= l.input :email, required: false %>
<%= l.input :password, required: false %>
<%= l.button :submit, 'Login!', class: 'btn-primary' %>
<% end %>
